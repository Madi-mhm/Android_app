import { StyleSheet } from 'react-native'

export const AppStyle = (StyleSheet.createstyle = StyleSheet.create({
  mainContainer: {
    width: '100%',
    height: '100%',
    backgroundColor: '#FCA311',
  },
}))